# These packages won't work on Vercel:
# - mkl-service (Intel Math Kernel Library - Linux version not available)
# - pywin32 (Windows-only package)
# - PyQt5 and PyQtWebEngine (GUI packages not needed for web apps)

# Here's a simplified requirements.txt for Django on Vercel:
asgiref==3.9.1
Django==5.2.5
gunicorn==23.0.0
sqlparse==0.5.3
whitenoise==6.11.0
psycopg2-binary==2.9.9
dj-database-url==2.1.0